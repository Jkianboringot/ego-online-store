<div>
    @foreach ($vars as $var )
    <p>{{$var}}</p>
    @endforeach
<br>
    @foreach ($makers as $maker )
    <p>{{$maker}}</p>
    @endforeach
<br>

    @foreach ($models as $model )
    <p>{{$model}}</p>
    @endforeach
</div>